# Core framework
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.6.1
python-multipart==0.0.19

# Agent orchestration
langgraph==0.2.60
langchain-core>=0.3.26,<0.4.0

# LLM gateway (OpenAI-compatible, multi-provider)
litellm==1.53.1
httpx==0.28.1
ollama>=0.4.0

# Vector store / lexical search
# Production vector store: the standalone Milvus server (compose service
# `milvus`). pymilvus 3.x drops the pkg_resources dependency (broken on
# Python 3.13); Milvus Lite (local file via LEGAL_AI_MILVUS_URI) is only a
# dev/test fallback — milvus-lite 3.x is pure Python (faiss-cpu backend),
# so it installs on every platform, including linux/arm64 containers.
# grpcio pinned alongside: pymilvus excludes the whole 1.68–1.73 line, and
# pinning spares pip the multi-version backtracking that stalls image builds.
pymilvus==3.0.1
grpcio==1.83.0
milvus-lite>=3.2.0
rank-bm25==0.2.2

# Datastores
redis==5.2.1
SQLAlchemy==2.0.36
greenlet>=3.1  # required by SQLAlchemy's async engine (not pulled without the [asyncio] extra)
aiosqlite==0.20.0
psycopg[binary]==3.2.3

# Security
PyJWT==2.10.1
bcrypt==4.2.1
cryptography==44.0.0

# Guardrails (optional semantic scanner — not included by default because it is
# heavy and not always available as a manylinux wheel. Install separately with:
#   pip install -r requirements-guardrails.txt
# The deterministic regex guards remain active even without this package.)
# llm-guard>=0.3.0,<1.0.0

# Observability
prometheus-client==0.21.1
opentelemetry-sdk==1.29.0
opentelemetry-exporter-otlp-proto-http==1.29.0
opentelemetry-instrumentation-fastapi==0.50b0
langfuse==2.57.0

# Ingestion
pypdf==5.1.0  # still used by backend/tools/pdf_parser.py; loaders use PyMuPDF
python-docx==1.1.2
beautifulsoup4==4.12.3
lxml==5.3.0
Markdown==3.7
feedparser==6.0.11

# OCR for scanned PDFs: PyMuPDF rendering + Tesseract (default) or PaddleOCR.
# Import-guarded everywhere (backend/ingestion/ocr.py), so the platform runs
# normally without these installed. paddlex (a paddleocr dep) hard-pins
# opencv-contrib-python, which needs the libgl system library; the Docker
# build swaps it for the pinned headless build below (same cv2 API, no libgl).
PyMuPDF==1.28.2
pytesseract==0.3.13
paddlepaddle==3.2.0
paddleocr==3.2.0
Pillow==11.3.0
opencv-contrib-python-headless==4.10.0.84

# Export (PDF/Word)
reportlab==4.1.0

# Speech-to-text (voice messages, POST /chat/transcribe). The default
# stt_provider is "litellm" (no extra dependency); faster-whisper powers the
# optional fully-local provider and is import-guarded in backend/core/stt.py.
# Its dependency tree (ctranslate2, onnxruntime, av, tokenizers) resolves to
# cp312 manylinux wheels — verified with pip download (see
# scripts/download_wheels.sh).
faster-whisper==1.2.1

# Testing
pytest==8.3.4
pytest-asyncio==0.25.0
