"""Temporal durability layer: workflows, activities, worker and client.

Everything degrades gracefully when temporalio is not installed or no
Temporal server is reachable — the rest of the system runs without it.
"""
