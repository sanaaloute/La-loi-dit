# expo-router v57 pulls web UI deps (vaul -> @radix-ui/*) whose peer ranges
# pin react-dom versions not installed in a native app. npm's strict peer
# resolution (and EAS Build's clean `npm ci`) fails with ERESOLVE without this.
legacy-peer-deps=true
